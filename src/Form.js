// import { useState } from "react";

function Form() {
    // No state variables are needed since there are no input fields

    return (
        <div>
            <form>
                {/* The form is now empty, you can add other fields if needed */}
                <button type="submit">Submit</button>
            </form>
        </div>
    );
}

export default Form;